{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Cabine</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nomcabine</th>
                <td>{{ entity.nomCabine }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('cabine') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('cabine_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
