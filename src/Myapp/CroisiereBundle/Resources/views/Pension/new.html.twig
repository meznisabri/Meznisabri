{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pension creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('pension') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
