{% extends '::base.html.twig' %}

{% block body -%}
    <h1>imageCros list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Url</th>
                <th>Alt</th>
                <th>File</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('imagecros_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.url }}</td>
                <td>{{ entity.alt }}</td>
                <td>{{ entity.file }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('imagecros_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('imagecros_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('imagecros_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
